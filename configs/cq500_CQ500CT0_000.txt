# generated by prepare_cq500.py  --  prepare_cq500.py --root /home/kctung/Projects/FFGS-benchmark/datasets/rendered_images/CQ500_processed_new --param-axes patient,tf --no-holdout
dataset_name = visnerf
dataset = cq500
datadir = /home/kctung/Projects/FFGS-benchmark/datasets/rendered_images/CQ500_processed_new/CQ500CT0_000_ViSNeRF
expname = cq500_CQ500CT0_000
basedir = ./log

input_res = [256,256]
output_res = [256,256]

# --- parameter axes: patient, tf ---
nParams = 1
n_lamb_params = [4]
vecSize_params = [1]     # = #distinct TRAIN values per axis
min_params = [0]
max_params = [0]       # full index range so held-out TFs interpolate

# --- scene fit (poses are canonicalized to radius 3.0 in the loader) ---
bbox = [[-1.5,-1.5,-1.5],[1.5,1.5,1.5]]
near_far = [1.0, 5.0]
# black background: leave white_bkgd unset

n_iters = 90000
batch_size = 4096

N_voxel_init = 2097152    # 128**3
N_voxel_final = 27000000  # 300**3
upsamp_list = [6000,9000,12000,16500,21000]
update_AlphaMask_list = [6000,12000]
use_AlphaMask = 0         # set 1 (paper) once geometry is verified

N_vis = 3
vis_every = 5000
render_test = 1
render_path = 0

n_lamb_sigma = [16,16,16]
n_lamb_sh = [48,48,48]
model_name = ViSNeRF

shadingMode = MLP_Fea
fea2denseAct = softplus
view_pe = 2
fea_pe = 2

L1_weight_inital = 8e-5
L1_weight_rest = 4e-5
rm_weight_mask_thre = 1e-4
TV_weight_density = 0
TV_weight_app = 1.0
